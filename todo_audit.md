# TODO (plan d’actions)

## Court terme
- [ ] Aligner les versions entre `config.py` et `analyse_conversations_merged.py`.
- [ ] Normaliser la liste des dépendances (`DEPENDANCES`) entre `config.py` et `install.py`.
- [ ] Compléter `README.md` avec guide d’installation + exemples.

## Moyen terme
- [ ] Factoriser `ensure_directory` dans `utils.py`.
- [ ] Ajouter un fichier de tests minimal (smoke tests CLI).

## Long terme
- [ ] Ajouter une documentation complète (structure, flux, formats).
- [ ] Mettre en place un pipeline CI pour lancer des tests automatiques.
